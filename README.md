# mdsplit
```
Usage: splitter [options]

Options:
-i, --input [file]     file path
-o, --output [folder]  output folder path
-l, --level <number>   heading level 1–6 (default: 1)
-t, --toc              table of contents
-f, --force            overwrite output
-s, --slugify          generate slugify filenames and folders
-v, --verbose          verbose output
-h, --help             display help for command
```